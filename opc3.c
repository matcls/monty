#include "monty.h"

/**
* op_mol - computes the rest of the division of the second top
* element of the stack by the top element of the stack.
* @stack: memory stack
* @line_number: command line number
*/
void op_mol(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
